<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cloud.dowhat.monolith.web.mapper.IMessageRecipientMapper">
    <insert id="insert">
        insert into t_message_recipient (message_id, recipient_id,type)
        values (#{ew.messageId}, #{ew.recipientId},#{ew.type});
    </insert>

    <update id="save">

    </update>
</mapper>
